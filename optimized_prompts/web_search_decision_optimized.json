{
  "generate.predict": {
    "traces": [],
    "train": [],
    "demos": [
      {
        "history_block": "None",
        "rag_context": "None",
        "rag_is_insufficient": "false",
        "message_urls": "None",
        "history_urls": "None",
        "message": "What refund window does the store offer for shoes that do not fit?",
        "action": "answer_from_rag",
        "reason": "context_is_relevant",
        "query": "",
        "url": "",
        "expected_output": "The store offers a 30-day refund window for unworn shoes, and customers can start the return from the orders page."
      }
    ],
    "signature": {
      "instructions": "Given the fields `history_block`, `rag_context`, `rag_is_insufficient`, `message_urls`, `history_urls`, `message`, produce the fields `action`, `reason`, `query`, `url`.",
      "fields": [
        {
          "prefix": "History Block:",
          "description": "${history_block}"
        },
        {
          "prefix": "Rag Context:",
          "description": "${rag_context}"
        },
        {
          "prefix": "Rag Is Insufficient:",
          "description": "${rag_is_insufficient}"
        },
        {
          "prefix": "Message Urls:",
          "description": "${message_urls}"
        },
        {
          "prefix": "History Urls:",
          "description": "${history_urls}"
        },
        {
          "prefix": "Message:",
          "description": "${message}"
        },
        {
          "prefix": "Reasoning:",
          "description": "${reasoning}"
        },
        {
          "prefix": "Action:",
          "description": "${action}"
        },
        {
          "prefix": "Reason:",
          "description": "${reason}"
        },
        {
          "prefix": "Query:",
          "description": "${query}"
        },
        {
          "prefix": "Url:",
          "description": "${url}"
        }
      ]
    },
    "lm": null
  },
  "metadata": {
    "dependency_versions": {
      "python": "3.13",
      "dspy": "3.2.1",
      "cloudpickle": "3.1"
    }
  }
}
